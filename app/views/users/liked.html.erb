<div class="row">
  <div class="col md-6 offset-3">
    <h1>
      <%= @user.username %>
    </h1>
  </div>
      <%= render partial: "shared/navs" %>
</div>

<% @user.liked_photos.each do |photo| %>
  <%= render "photos/photo", photo: photo %>
  <% end %>
